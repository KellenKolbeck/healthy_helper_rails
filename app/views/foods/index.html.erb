<h1>Foods:</h1>

<% @foods.each do |food| %>
  <%= food.name.split(" ").map(&:capitalize).join(" ") %> <%= link_to "Edit", edit_food_path(food) %>
  <%= link_to "Delete", food_path(food), :data => {confirm: "Are Your Sure?", method: "delete"}, class: "btn btn-danger" %>
<% end %>


<%= link_to "New Food", new_food_path %>
